/**
 * Function: showAnimalInfo
 * Purpose is to show an alert with the animal's name when a card is clicked.
 * Parameters:
 * - name (string) = The name of the animal to show in the alert.
 */
function showAnimalInfo(name) {
    alert("You clicked on " + name + "!");
  }